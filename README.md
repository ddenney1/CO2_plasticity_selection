# CO2_plasticity_selection

GC_pops_map.R uses the GC_Pops.csv file to output a topographical map (figure 1).

Denney_Anderson_plasticity_selection.R is an update to trait_selection.R.
It uses fitness_data.csv to generate figures 2-6
